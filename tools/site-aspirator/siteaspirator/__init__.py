"""site-aspirator — extraction/aspiration de code source + navigation via CDP.

Modules : cdp (canal), protocol (adaptation), capture (extraction),
memory (mémorisation locale), report (rapport). Orchestré par aspirator.py.
"""

__version__ = "1.0.0"
