[user] Tu es l'ARCHITECTE EN CHEF JARVIS. Fusionne en une v2 cohérente :
RECHERCHE(SOTA) = {"findings":[{"claim":"A1 — Gateway LLM unique (jarvis-agent = seul point de passage vers tout modèle). L'état de l'art traite ça comme un principe d'ARCHITECTURE (economy of mechanism + complete mediation de Saltzer-Schroeder : un seul chokepoint où tout appel LLM est médié), MAIS avertit qu'un gateway unique n'est PAS en soi une défense anti-injection. La vraie sécurité vient de la séparation planificateur-privilégié / lecteur-quarantiné (Dual LLM, CaMeL). A1 fournit le chokepoint QUI PERMET d'imposer ce split, mais ne l'impose pas. Verdict: fondation de médiation valide, complétude sécurité à préciser.","source":"Willison, 'The Dual LLM pattern' (2023) + Beurer-Kellner et al., 'Design Patterns for Securing LLM Agents against Prompt Injections', arXiv:2506.08837 (2025) — le Controller/orchestrateur non-LLM est le vrai garant, pas la centralisation du transport. Saltzer & Schroeder (1975) 'complete mediation' / 'economy of mechanism'.","verdict":"nuance"},{"claim":"A1-bis — agent = nœud feuille (out_degree=0, casse le cycle agent↔publish, seul Ω orchestre = DAG acyclique). C'est EXACTEMENT le principe directeur central de l'état de l'art : 'une fois qu'un agent LLM a ingéré une entrée non fiable, il doit être contraint de sorte qu'il soit IMPOSSIBLE que cette entrée déclenche une action conséquente' — soit briser la boucle de rétroaction sortie-action → contrôle. C'est le pattern Action-Selec

[assistant] I'll quickly verify the key file paths exist so my todolist references real locations, then synthesize the v2.

[assistant] Paths confirmed real. Let me locate the numbered pack docs (the actual targets to engrave into) and confirm the ops/audit script dirs.

[assistant] All paths confirmed. Real targets: `03-PROMPT-EXPORT.md` (where §9 lands), `01-MATRICE-VECTORIELLE.md` (M1/M2 fixes), `04-CARTE-MENTALE.md` (§À épaissir); `scripts/lib/` (envelope/errors/proc absent → to create); existing `events.py`/`trust.py`/`env.py`; `scripts/audit/check-a1-gateway.py`; `scripts/ops/drill.py`; `bin/jarvis*` CLIs. Now synthesizing the v2.