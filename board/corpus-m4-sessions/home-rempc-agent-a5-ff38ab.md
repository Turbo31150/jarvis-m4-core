[user] Rédige la SPEC DE CLÔTURE (markdown, concret, contrats exécutables) du gap suivant pour l'écosystème JARVIS. Respecte impérativement les 4 lois de frontières et le DAG (lis /home/rempc/jarvis/docs/exports/pack-methode-jarvis/01-MATRICE-VECTORIELLE.md avant). Zéro invention hors logique établie. GAP #1 : Contrats exécutables : enveloppe I/O JSON standard + exit codes + codes d'erreur, par brique

[assistant] I'll read the required matrix file and explore the pack directory to understand the boundary laws and DAG before writing the closure spec.

[assistant] Let me read the surrounding pack files to align the closure spec with the pack's conventions and to verify the real contracts referenced.

[assistant] Let me probe the real code to ground the contracts in what actually exists (zero invention rule).

[assistant] Let me read the actual brick binaries and the safety-floor spec to ground the envelope and error conventions.

[assistant] Let me check whether the Python scripts already have a shared output/error convention so the envelope grounds in reality, not invention.

[assistant] Let me read the actual CLI output/error patterns and the shared helpers to ground the envelope precisely.

[assistant] Let me check the action_schema, events lib, and mem/web/board JSON shapes to lock the envelope to real field names.

[assistant] I have everything I need. I've read the required matrix (M1 frontiers, M2 DAG, V vectors, A algorithm, Σ invariants), the pack context, the safety-floor spec, and the real brick code (exit codes 0/1/2 observed, `--json` shapes, `_pub_common`, `action_schema`, `events.py`, `guard.py` SSRF, the board "empty vs exit≠0" hors-chantier bug). Now I'll write the closure spec, grounding every field/code in observed reality and mapping it onto the 4 laws + DAG.