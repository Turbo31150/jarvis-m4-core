[user] Rédige la SPEC DE CLÔTURE (markdown, concret, contrats exécutables) du gap suivant pour l'écosystème JARVIS. Respecte impérativement les 4 lois de frontières et le DAG (lis /home/rempc/jarvis/docs/exports/pack-methode-jarvis/01-MATRICE-VECTORIELLE.md avant). Zéro invention hors logique établie. GAP #2 : Moteur d'orchestration : spécifier 'flow' + Claude=chef comme composant (DAG, policy, audit, dry-run, idempotence)

[assistant] I'll start by reading the required matrix file to understand the 4 boundary laws and DAG before writing the closure spec.

[assistant] Let me explore the pack directory and the actual flow implementation to keep contracts executable and non-invented.

[assistant] Let me read the sibling pack files and the actual `jarvis` launcher to match conventions and keep contracts real.

[assistant] Let me check for any existing closure-spec format and confirm real referenced scripts exist.

[assistant] Let me read the safety-floor spec to reuse the real idempotence/hash and drill concepts (avoid invention).

[assistant] I have everything I need: the 4 boundary laws (A1–A4) + A0/A5, the DAG M2 (only Ω→publish, agent = leaf, acyclic), invariants Σ, the flow algorithm A, the real `jarvis flow` launcher (`claude -p "/jarvis-flow"`, `JARVIS_COMMAND_ID` correlation at line 14), the real ops scripts (events.py, drill.py), and the Safety Floor (stage/commit SHA-256 idempotence #3, 3 states, drill #4). Now I'll write the closure spec, anchoring every element to an established mechanism.