# Commandes M1 — gpu-materiel

## A QUOI CA SERT

Etat materiel de la machine. Memoire, disque, temperature et charge GPU, partitions, swap. Sert quand la machine rame, gele ou sature.

## COMMANDES REELLEMENT EXECUTEES

Extraites de 1840 sessions Claude Code sur M1, triees par frequence.
Un chemin en /home/turbo ou une IP 192.168.0.10 doit etre reecrit avant rejeu sur M4.

- (5x) `df -h /`
- (3x) `df -h / | tail -1`
- (1x) `which sensors 2>/dev/null && sensors 2>/dev/null | head -40; echo "---libsensors---"; ls /usr/lib/x86_64-linux-gnu/libsensors* 2>/dev/null; echo "---hwmon---"; for d in /sys/class/hwmon/hwmon*; do printf "%s %s\n" "$d" "$(cat $d/name 2>/dev/null)"; done`
- (1x) `uptime; free -m | awk 'NR==2{printf "RAM: %d/%d MB (%.0f%%)\n",$3,$2,$3*100/$2}'; nvidia-smi --query-gpu=index,temperature.gpu --format=csv,noheader 2>/dev/null`
- (1x) `uptime; free -m | awk 'NR==2{printf "RAM: %d%% (%d/%d Mo)\n", $3*100/$2, $3, $2}'; nvidia-smi --query-gpu=temperature.gpu --format=csv,noheader | sort -n | tail -1 | xargs echo 'GPU max °C:'; df -h / /mnt/jarvis-data 2>/dev/null | awk 'NR>1{print $6, $5}'`
- (1x) `uptime; echo "tainted=$(cat /proc/sys/kernel/tainted)"; echo "CPU: $(nproc) coeurs"; free -h | head -2 | tail -1; echo; echo "workflow deep-research encore actif ?"; pgrep -fc "deep-research" 2>/dev/null || echo 0`
- (1x) `uptime; echo "=== Secure Boot / MOK ==="; mokutil --sb-state 2>&1; mokutil --test-key /var/lib/shim-signed/mok/MOK.der 2>&1; echo "=== NVIDIA ==="; nvidia-smi 2>&1 | head -12`
- (1x) `uptime; echo "--- RAM ---"; free -m | head -2; echo "--- GPU ---"; nvidia-smi --query-gpu=index,temperature.gpu,memory.used,memory.total --format=csv,noheader 2>/dev/null`
- (1x) `uptime; echo "--- MEM ---"; free -m | head -3; echo "--- ZOMBIES ---"; ps -eo stat,ppid,pid,comm | awk '$1 ~ /Z/' | head -10; echo "--- TOP CPU ---"; ps -eo pcpu,pmem,pid,comm --sort=-pcpu | head -8; echo "--- GPU ---"; nvidia-smi --query-gpu=index,name,temperature.gpu,memory.used,memory.total,utilization.gpu --format=csv,noheader 2>&1 | head -6`
- (1x) `uptime | grep -oE "load average.*"; free -m | awk '/Mem:/{printf "RAM %.0f%%\n",$3/$2*100}'; /usr/bin/nvidia-smi --query-gpu=index,temperature.gpu --format=csv,noheader | tr '\n' ' '; echo; pgrep -cf python3`
- (1x) `timeout 40 lms unload --all 2>&1 | tail -2 ; sleep 12 ; echo "═══ températures après déchargement ═══" ; nvidia-smi --query-gpu=index,temperature.gpu,fan.speed,memory.used --format=csv,noheader`
- (1x) `tail -6 ~/jarvis/logs/lms-runaway-guard.log ; echo "--- état actuel ---"; timeout 20 lms ps 2>&1 | head -4 ; echo "--- VRAM ---"; nvidia-smi --query-gpu=index,memory.used,temperature.gpu --format=csv,noheader`
- (1x) `swapon --show; echo "---STAT SWAPFILES---"; sudo stat -c '%n links=%h taille=%s' /swap-nvme.img /swapfile_emergency /timeshift/snapshots/2026-07-23_19-40-22/localhost/swap-nvme.img /timeshift/snapshots/2026-07-16_08-02-08/localhost/swap-nvme.img /timeshift/snapshots/2026-07-23_19-40-22/localhost/swapfile_emergency /timeshift/snapshots/2026-07-16_08-02-08/localhost/swapfile_emergency 2>/dev/null`
- (1x) `sudo nvidia-smi -i 2 -c PROHIBITED 2>&1 | tail -2`
- (1x) `sudo -n true 2>/dev/null && echo "SUDO: OK sans mot de passe" || echo "SUDO: mot de passe requis"; echo "--- paquets déjà présents ---"; dpkg -l 2>/dev/null | grep -E '^ii +(lm-sensors|i2c-tools|dkms|openrgb|liquidctl|fancontrol)' | awk '{print $2, $3}'; echo "--- dkms status ---"; dkms status 2>/dev/null | head`
- (1x) `stat -c '%s %y' /home/turbo/jarvis/backups/boot-repair-20260806/e2fsck-backupos-run2.log; df -h / | tail -1`
- (1x) `set -e ; # Rien n'est utilise (0B partout) -> reordonnancement sans risque ; sudo swapoff /dev/zram0 && sudo swapon -p 100 /dev/zram0 ; sudo swapoff /dev/nvme0n1p1 && sudo swapon -p 20 /dev/nvme0n1p1 ; sudo swapoff /dev/dm-0 && sudo swapon -p 10 /dev/dm-0 ; echo "=== NOUVEL ORDRE ==="; swapon --show`
- (1x) `set +e ; # 1. Suspendre immediatement les gros consommateurs (SIGSTOP = gel instantane, reversible) ; for p in $(ps -eo pid,pcpu,comm --sort=-pcpu | awk 'NR>1 && $2>15 && $3!~/claude|bash|ps/ {print $1}' | head -20); do ;   kill -STOP $p 2>/dev/null && echo "gele pid $p ($(cat /proc/$p/comm 2>/dev/null))" ; done ; echo "--- load apres gel ---"; cat /proc/loadavg; sensors -A 2>/dev/null | grep Tctl`
- (1x) `sensors k10temp-pci-00c3 2>&1 | head -12; echo "=== all chips ==="; sensors 2>/dev/null | grep -E '^[a-z0-9_]+-'`
- (1x) `sensors 2>/dev/null | sed -n '/nct6687/,/^$/p'; echo "=== VENTILOS BRUTS ==="; for f in /sys/class/hwmon/hwmon6/fan*_input; do [ -f "$f" ] && echo "$(basename $f): $(cat $f) RPM"; done; echo "=== PWM ==="; for p in /sys/class/hwmon/hwmon6/pwm[0-9]; do [ -f "$p" ] && echo "$(basename $p): $(cat $p)/255 ($(( $(cat $p)*100/255 ))%) mode=$(cat ${p}_enable 2>/dev/null)"; done`
- (1x) `sensors 2>/dev/null | head -60; echo "---"; ls /sys/class/hwmon/ 2>/dev/null`
- (1x) `sensors 2>/dev/null | grep -A12 -i k10temp; echo "=== sysfs ==="; for d in /sys/class/hwmon/hwmon*; do n=$(cat $d/name 2>/dev/null); if [ "$n" = "k10temp" ]; then echo "--- $d ($n)"; ls $d | grep -E '^temp'; for f in $d/temp*_label $d/temp*_input; do echo "$f = $(cat $f 2>/dev/null)"; done; fi; done`
- (1x) `sensors 2>/dev/null | awk ' ; /^[a-z].*-/ {chip=$0} ; /\+[89][0-9]\.[0-9]°C|\+1[0-9][0-9]/ {print "  ["chip"] "$0} ; ' | head -8 ; echo "--- NVMe / disques ---" ; sensors 2>/dev/null | grep -B1 -A3 -i nvme | head -10`
- (1x) `sensors 2>&1 | head -80`
- (1x) `sensors 2>&1 | head -60; echo "---"; ls /sys/class/hwmon/ 2>/dev/null | while read h; do echo "$h: $(cat /sys/class/hwmon/$h/name 2>/dev/null)"; done; echo "---"; dpkg -l 2>/dev/null | grep -Ei 'lm-sensors|libsensors|libgtop|libglib2' | head`
- (1x) `sensors -A 2>/dev/null | grep -E "Tctl|Tccd"; cat /proc/loadavg`
- (1x) `sed -n '1,25p' /home/turbo/jarvis/bin/reports-reindex.py ; echo "--- LIBDIR mount ---"; LIB=$(grep -oP 'LIBDIR\s*=\s*Path\("\K[^"]+' /home/turbo/jarvis/bin/reports-reindex.py 2>/dev/null); echo "LIBDIR=$LIB"; df -h "$LIB" 2>&1; df -i "$LIB" 2>&1; findmnt -T "$LIB" 2>&1`
- (1x) `python3 ~/jarvis/bin/skillmp-pipeline.py status 2>&1 | tail -12; echo "---disk---"; df -h / | tail -1`
- (1x) `python3 cli/biblio_filler.py --status 2>&1 | head -25; echo "--- LOAD ---"; uptime; nvidia-smi --query-gpu=index,temperature.gpu --format=csv,noheader 2>/dev/null`
- (1x) `python3 /home/turbo/jarvis/bin/skillmp-pipeline.py stop boucle fr 2>&1 | tail -3 ; echo "── vérification : tout est arrêté ? ──" ; python3 /home/turbo/jarvis/bin/skillmp-pipeline.py status 2>&1 | grep -E "actif|arrete" | head -7 ; echo "── charge / GPU ──"; uptime | grep -oE "load average.*"; /usr/bin/nvidia-smi --query-gpu=index,temperature.gpu --format=csv,noheader | tr '\n' ' '`
- (1x) `ps -o pid,ppid,etime,args -p 2991485 2>/dev/null | tail -1 | cut -c1-90; kill 2991485 2>/dev/null && echo "worker hérité arrêté"; sleep 3; nvidia-smi --query-gpu=index,memory.used,temperature.gpu --format=csv,noheader`
- (1x) `pgrep -af 'skillmp' | grep -v pgrep | sed 's/ .*skillmp/ skillmp/' | cut -c1-90 | head -10; echo "---disk---"; df -h / | tail -1`
- (1x) `nvidia-smi 2>&1 | head -60`
- (1x) `nvidia-smi 2>&1 | head -5; echo "---"; uname -r; echo "---"; lsmod | grep -i nvidia | head; echo "---"; mokutil --sb-state 2>&1; echo "---"; dkms status 2>&1`
- (1x) `nvidia-smi 2>&1 | head -30; echo "=== RC=$? ==="; echo "--- modules ---"; lsmod | grep -i nvidia; echo "--- version ---"; cat /proc/driver/nvidia/version 2>&1`
- (1x) `nvidia-smi -q 2>&1 | head -400`
- (1x) `nvidia-smi -q -i 2 2>&1 | sed -n '1,40p;/Temperature/,/Power Samples/p' | head -120`
- (1x) `nvidia-smi -q -d POWER | grep -A3 "Power Limit\|Persistence"; echo "---"; nvidia-smi --query-gpu=index,name,persistence_mode,power.draw,power.limit,display_active --format=csv`
- (1x) `nvidia-smi -i 2 --query-gpu=index,temperature.gpu,clocks.gr,clocks.max.gr,power.draw --format=csv,noheader 2>/dev/null ; echo "--- GPU2 sous cap : max clock plafonné à 900 MHz → ne peut plus thermal-runaway ---"`
- (1x) `nvidia-smi -L; echo "---mémoire par GPU---"; nvidia-smi --query-gpu=index,uuid,memory.used,temperature.gpu --format=csv,noheader`
- (1x) `nvidia-smi --query-gpu=index,temperature.gpu,utilization.gpu,memory.used,memory.total --format=csv,noheader 2>&1`
- (1x) `nvidia-smi --query-gpu=index,temperature.gpu,utilization.gpu,memory.used,fan.speed --format=csv,noheader 2>&1 | sed 's/^/  /' ; echo "RAM: $(free -m|sed -n 2p|awk '{printf "%d%% (%d Mo dispo)",$3*100/$2,$7}') · load:$(uptime|grep -o 'average.*'|cut -c9-20)"`
- (1x) `nvidia-smi --query-gpu=index,temperature.gpu,power.draw,memory.used,fan.speed --format=csv,noheader; echo "--- procs GPU ---"; nvidia-smi --query-compute-apps=gpu_uuid,pid,process_name,used_memory --format=csv,noheader 2>/dev/null | head`
- (1x) `nvidia-smi --query-gpu=index,temperature.gpu,fan.speed --format=csv,noheader 2>&1 | head -5`
- (1x) `nvidia-smi --query-gpu=index,temperature.gpu --format=csv,noheader 2>/dev/null || echo "GPU query failed"`
- (1x) `nvidia-smi --query-gpu=index,pci.bus_id,temperature.gpu,memory.used,utilization.gpu --format=csv,noheader; echo '--- process GPU ---'; nvidia-smi --query-compute-apps=gpu_bus_id,pid,used_memory --format=csv,noheader | awk '{print}' | head -8`
- (1x) `nvidia-smi --query-gpu=index,name,uuid,pci.bus_id,vbios_version --format=csv 2>&1`
- (1x) `nvidia-smi --query-gpu=index,name,uuid,pci.bus_id,fan.speed,temperature.gpu,power.draw,memory.total --format=csv 2>&1`
- (1x) `nvidia-smi --query-gpu=index,name,uuid,pci.bus_id,compute_mode,memory.total,driver_version --format=csv 2>&1; echo "---LSPCI---"; lspci -nn | grep -Ei 'vga|3d|nvidia' 2>&1`
- (1x) `nvidia-smi --query-gpu=index,name,uuid,memory.total,memory.used,memory.free,temperature.gpu,power.draw --format=csv 2>&1`
- (1x) `nvidia-smi --query-gpu=index,name,temperature.gpu,utilization.gpu,memory.used,power.draw --format=csv,noheader; echo '---top process GPU---'; nvidia-smi --query-compute-apps=gpu_uuid,pid,process_name,used_memory --format=csv,noheader | head -6`
- (1x) `nvidia-smi --query-gpu=index,name,temperature.gpu,utilization.gpu,memory.used,memory.total,fan.speed,power.draw,pstate --format=csv 2>&1; echo "=== ECC/XID ==="; sudo -n dmesg 2>/dev/null | grep -iE "xid|nvrm|nvidia-modeset.*Error" | tail -15 || echo "dmesg sudo indispo"`
- (1x) `nvidia-smi --query-gpu=index,name,temperature.gpu,fan.speed,utilization.gpu,memory.used,power.draw,compute_mode --format=csv,noheader 2>/dev/null`
- (1x) `nvidia-smi --query-gpu=index,name,temperature.gpu,fan.speed,power.draw,memory.used,utilization.gpu --format=csv,noheader ; echo "--- seuils ---"; nvidia-smi --query-gpu=index,temperature.gpu.tlimit --format=csv,noheader 2>/dev/null | head -4`
- (1x) `nvidia-smi --query-gpu=index,name,temperature.gpu,fan.speed,power.draw,memory.used,utilization.gpu --format=csv,noheader`
- (1x) `nvidia-smi --query-gpu=index,name,power.limit,power.min_limit,power.max_limit,temperature.gpu --format=csv,noheader`
- (1x) `nvidia-smi --query-gpu=index,name,pci.bus_id,temperature.gpu,fan.speed,power.draw,power.limit,power.max_limit,clocks.sm,clocks_throttle_reasons.active,driver_version,vbios_version,serial,uuid --format=csv 2>&1`
- (1x) `nvidia-smi --query-gpu=index,name,memory.used,memory.total,utilization.gpu,temperature.gpu --format=csv,noheader`
- (1x) `nvidia-smi --query-gpu=index,name,memory.used,memory.total --format=csv 2>&1 | head -8; echo "=== proc lms ==="; ps -eo pid,lstart,etime,comm,args 2>/dev/null | grep -i "llama-server\|lms\|LM Studio" | grep -v grep | head -5 | cut -c1-200`
- (1x) `nvidia-smi --query-gpu=index,name,memory.used --format=csv,noheader; echo "---proc GPU4---"; nvidia-smi --query-compute-apps=gpu_uuid,pid,used_memory --format=csv,noheader 2>/dev/null | head`
- (1x) `nvidia-smi --query-gpu=index,name,compute_mode,memory.used,memory.total --format=csv,noheader 2>&1; echo "---CUDA env LMS---"; pgrep -af 'lm-studio|LM Studio' | head -3; for p in $(pgrep -f 'lm-studio' | head -1); do tr '\0' '\n' < /proc/$p/environ | grep -i cuda; done 2>/dev/null`
- (1x) `nvidia-smi --query-gpu=index,memory.used,memory.total --format=csv,noheader; lms load hermes-2-pro-mistral-7b --context-length 4096 --ttl 86400 -y 2>&1 | tail -1 | tr '\r' '\n' | tail -2`
- (1x) `nvidia-smi --query-gpu=index --format=csv,noheader | wc -l`
- (1x) `nvidia-smi --query-gpu=driver_version,vbios_version,serial,uuid,pci.bus_id --format=csv 2>&1; echo "---SENSORS---"; sensors 2>&1 | head -60`
- (1x) `nvidia-smi --query-compute-apps=pid,used_memory,process_name --format=csv,noheader 2>/dev/null | head -8 ; echo "=== correspondance PID → service ===" ; for p in $(nvidia-smi --query-compute-apps=pid --format=csv,noheader 2>/dev/null | head -5); do ;   echo "$p: $(ps -p $p -o comm=,etime= 2>/dev/null | tr -s ' ')" ; done`
- (1x) `nvidia-smi --query-compute-apps=pid,process_name,used_memory,gpu_uuid --format=csv; echo "---GPU LIST---"; nvidia-smi --query-gpu=index,uuid,name --format=csv`
- (1x) `nvidia-smi --query-compute-apps=pid,process_name,used_memory,gpu_uuid --format=csv`
- (1x) `nvidia-smi --query-compute-apps=pid,gpu_uuid,used_memory --format=csv,noheader 2>/dev/null | sed 's/GPU-27[^,]*/RTX3080(GPU3)/;s/GPU-d0[^,]*/1660S(GPU1)/;s/GPU-[a-f0-9-]*/GPU0-ou-2/' ; nvidia-smi --query-gpu=index,temperature.gpu,utilization.gpu,power.draw --format=csv,noheader`
- (1x) `nvidia-smi --query-compute-apps=pid,gpu_uuid,used_memory --format=csv,noheader 2>&1; echo "=== nommage ==="; for p in $(nvidia-smi --query-compute-apps=pid --format=csv,noheader 2>/dev/null); do printf "%s -> " "$p"; ps -o cmd= -p $p 2>/dev/null | cut -c1-160 || echo "(disparu)"; done`
- (1x) `nvidia-smi --query-compute-apps=pid,gpu_uuid,used_memory --format=csv 2>&1; echo "---GPU UUID MAP---"; nvidia-smi --query-gpu=index,uuid,name,temperature.gpu,utilization.gpu,memory.used,memory.total,fan.speed,power.draw --format=csv 2>&1`
- (1x) `nvidia-smi --query-compute-apps=pid,gpu_uuid,used_memory --format=csv 2>&1`
- (1x) `nvidia-smi --query-compute-apps=gpu_uuid,pid,used_memory --format=csv,noheader 2>/dev/null | while IFS=',' read -r u p m; do n=$(ps -o comm= -p $(echo $p|tr -d ' ') 2>/dev/null); idx=$(nvidia-smi --query-gpu=index,uuid --format=csv,noheader 2>/dev/null | grep "$(echo $u|tr -d ' ')" | cut -d, -f1); echo "  GPU$idx : $n ($m)"; done`
- (1x) `nvidia-smi --query-compute-apps=gpu_uuid,pid,used_memory --format=csv,noheader 2>/dev/null | grep 276cf5c1; echo "rc=$?"; ps -o cmd= -p 94429 2>/dev/null | cut -c1-80`
- (1x) `nvidia-smi --query-compute-apps=gpu_uuid,pid,process_name,used_memory --format=csv,noheader 2>/dev/null | sed 's/GPU-[a-f0-9-]*/GPU/' ; echo "=== parents des llama-server ==="; pgrep -x llama-server | while read p; do echo "  pid=$p parent=$(ps -o comm= -p $(ps -o ppid= -p $p|tr -d ' ') 2>/dev/null)"; done`
- (1x) `nvidia-smi --query-compute-apps=gpu_bus_id,pid,used_memory --format=csv,noheader; echo '---'; pgrep -af llama-server | head -5; echo '---'; nvidia-smi --query-gpu=index,pci.bus_id,temperature.gpu --format=csv,noheader`
- (1x) `nproc; echo "---load---"; uptime; echo "---mem---"; free -h`
- (1x) `nproc; cat /proc/loadavg; free -g | awk 'NR==2{print "RAM utilisee:",$3"G / "$2"G"}'`
- (1x) `lsblk -o NAME,SIZE,FSTYPE,UUID,MOUNTPOINT,MODEL 2>/dev/null; echo "==="; df -h | grep -vE "tmpfs|efivarfs|overlay"`
- (1x) `lsblk -o NAME,SERIAL,SIZE,FSTYPE,UUID,PARTUUID,MOUNTPOINT 2>&1`
- (1x) `ls -lh /swap-nvme.img 2>/dev/null; sudo df -h /swap-nvme.img 2>/dev/null; echo "--- df / ---"; df -h /`
- (1x) `ls -la scripts/chat_proxy.js && wc -l scripts/chat_proxy.js; uptime; nvidia-smi --query-gpu=index,name,temperature.gpu,utilization.gpu,memory.used --format=csv 2>/dev/null`
- (1x) `ls -la /usr/local/bin/nvidia-smi /usr/local/sbin/jarvis-gpu-lock-check /etc/jarvis/gpu-lock.json /etc/jarvis/gpu-guard.hash /var/log/jarvis-gpu-guard.log 2>&1; echo "---PATH---"; echo $PATH; echo "---SUDO---"; sudo -n true 2>&1 && echo "sudo NOPASSWD ok" || echo "sudo needs pw"`
- (1x) `ls -la /home/turbo/jarvis/board/recherche/ 2>/dev/null | tail -4 ; printf "agents actifs : %s · charge %s · GPU %sC\n" "$(pgrep -fc claude)" "$(cut -d' ' -f1 /proc/loadavg)" "$(nvidia-smi --query-gpu=temperature.gpu --format=csv,noheader 2>/dev/null|sort -rn|head -1)"`
- (1x) `lms ps 2>/dev/null; lms unload --all 2>&1 | tail -3; sleep 5; nvidia-smi --query-gpu=index,temperature.gpu,memory.used --format=csv,noheader; grep -n 'CUDA_VISIBLE' /home/turbo/jarvis/scripts/jlms.sh | head -5`
- (1x) `lms ps 2>&1 | tail -3; echo "---VRAM---"; nvidia-smi --query-gpu=index,memory.used,memory.total --format=csv,noheader`
- (1x) `kill 2655837 2>/dev/null; sleep 6; nvidia-smi --query-compute-apps=pid,gpu_uuid,used_memory --format=csv | grep 276cf5c1; echo "=== test nettoyé ==="`
- (1x) `kill -TERM 376509 2>/dev/null; sleep 8 ; ps -p 376509 >/dev/null 2>&1 && { echo "resiste -> SIGKILL"; kill -9 376509 2>/dev/null; sleep 5; } || echo "orphelin parti" ; echo "=== VRAM après nettoyage ===" ; nvidia-smi --query-gpu=index,memory.used,memory.total --format=csv,noheader 2>/dev/null | head -4`
- (1x) `kill -9 718192 2>/dev/null && echo "orphelin 718192 tue" ; timeout 5 tail -f /dev/null; nvidia-smi --query-gpu=index,memory.used --format=csv,noheader`
- (1x) `hostname; uname -a; nvidia-smi --query-gpu=index,name,pci.bus_id,temperature.gpu,power.draw,power.limit,power.min_limit,power.max_limit,fan.speed,utilization.gpu,clocks.sm,pstate --format=csv 2>&1`
- (1x) `hostname; nvidia-smi --query-gpu=index,name,temperature.gpu,power.draw,power.limit,power.min_limit,power.max_limit,fan.speed,utilization.gpu,clocks.sm,pcie.link.gen.current,pcie.link.width.current --format=csv 2>&1 | head -20`
- (1x) `grep -rn "CUDA_VISIBLE_DEVICES" /etc/environment /etc/profile.d/*.sh ~/.bashrc ~/.profile ~/.config/environment.d/*.conf /etc/systemd/system/*.service 2>/dev/null | head -20; echo "--- compute mode ---"; nvidia-smi --query-gpu=index,name,compute_mode,persistence_mode --format=csv; echo "--- lms process ---"; ps aux | grep -iE 'lm-studio|LM Studio|lms' | grep -v grep | head -5`
- (1x) `grep -nE "REGISTRY|^ROOT|Path\(" /home/turbo/jarvis/bin/reports-reindex.py 2>&1 | head -20 ; echo "--- df de toutes les cibles tmpfs ---"; df -h | grep -E "tmpfs|Sys" ; echo "--- test écriture réelle ---"; python3 -c " ; import sys; sys.path.insert(0,'/home/turbo/jarvis/bin') ; " 2>&1 ; echo "--- rerun manuel pour voir le chemin ---"; python3 bin/reports-reindex.py 2>&1 | tail -20`
- (1x) `grep -i huge /proc/meminfo; echo "---"; nvidia-smi --query-gpu=index,name,display_active,display_mode --format=csv; echo "--- xorg BusID"; grep -i busid /etc/X11/xorg.conf 2>/dev/null`
- (1x) `gh api repos/NVIDIA/k8s-device-plugin/issues/941/comments --jq '.[] | "=== \(.user.login) | \(.created_at) ===\n\(.body)\n"' 2>&1 | grep -n -i -E "k8s at all|535\.|driver version|nvidia-smi|GPU|reddit|4 x|4x|Tesla|RTX|3090|4090" | head -60`
- (1x) `free -m | sed -n 2p | awk '{printf "RAM %d%% · dispo %d Mo · swap ",$3*100/$2,$7}'; free -m | sed -n 3p | awk '{print $3" Mo"}' ; ps -eo rss,comm --sort=-rss | head -4 | awk 'NR>1{printf "  %.1f Go %s\n",$1/1048576,$2}' ; echo "process pipeline : $(ps -eo rss,cmd|grep -E 'aspire|github_source|expansion\.py|enrichir_fr'|grep -v grep|awk '{s+=$1}END{printf "%.0f Mo",s/1024}')"`
- (1x) `free -m | head -2; uptime | cut -d, -f3-; pgrep -c -f 'jarvis-prod-exec|executor-' 2>/dev/null`
- (1x) `free -m | awk '/Mem:/{printf "RAM %.0f%% (%s/%s Mo)\n",$3/$2*100,$3,$2}' ; swapon --show=NAME,SIZE,USED --noheadings 2>/dev/null | awk '{print "  swap "$1" "$3"/"$2}' ; ps aux --sort=-%mem 2>/dev/null | awk 'NR>=2&&NR<=4{printf "  %5.1f%% %6.0fMo %s\n",$4,$6/1024,$11}'`
- (1x) `free -h; echo "---SWAPON---"; swapon --show; echo "---ZRAMCTL---"; zramctl; echo "---LOAD---"; cat /proc/loadavg; uptime`
- (1x) `free -h | awk '/Mem:|Swap:/{print "  "$1" "$3"/"$2" utilise"}'; swapon --show 2>/dev/null | tail -3 ; echo "── top mem ──"; ps aux --sort=-%mem 2>/dev/null | awk 'NR>=2&&NR<=6{printf "  %5.1f%% %6.0fMB  %s\n",$4,$6/1024,$11}'`
- (1x) `free -h | awk '/Mem:/{print "RAM utilisée: "$3"/"$2"  libre: "$4"  dispo: "$7}' ; echo "load: $(cut -d' ' -f1-3 /proc/loadavg)" ; echo -n "collecteurs actifs: "; pgrep -cf "python3.*(aspire|expansion|github_source|avale_sections)"  ; echo -n "chrome/browseros déjà lancé: "; pgrep -c "chrome|chromium" 2>/dev/null || echo 0`
- (1x) `for p in 113147 715462 759005; do ;   echo "--- pid $p (démarré: $(ps -o lstart= -p $p 2>/dev/null | xargs)) ---" ;   tr '\0' '\n' < /proc/$p/cmdline 2>/dev/null | grep -A1 -E '^(--model|-ngl|--n-gpu-layers|--ctx-size)$' | paste - - | head -5 ; done ; echo "=== VRAM par GPU (lecture seule) ==="; nvidia-smi --query-gpu=index,name,memory.used,memory.total,temperature.gpu --format=csv,noheader 2>/dev/null`
- (1x) `for i in 1 2 3; do printf "%s  " "$(sensors -A 2>/dev/null | grep Tctl | tr -s ' ')"; done; echo; echo "--- tensions carte mere ---"; sensors -A 2>/dev/null | grep -E "VIN|3VSB|AVSB|VCC|Vcore|VSB" | head -12`
- (1x) `for i in 1 2 3; do echo "=== smi$i ==="; nvidia-smi -q -i $i -d PERFORMANCE,POWER 2>&1 | grep -E "Slowdown|Idle|SW Power Cap|HW Thermal|Sync|Active|Power Limit|Instantaneous" | head -20; done`
- (1x) `for i in 1 2 3; do echo "=== GPU $i ==="; nvidia-smi -q -i $i 2>&1 | grep -A14 "^    Fan Speed\|Fan Speed  " | head -20; done; echo "--- FAN block raw ---"; nvidia-smi -q 2>&1 | grep -n -i "fan" | head -40`
- (1x) `for h in /sys/class/hwmon/hwmon*; do n=$(cat $h/name 2>/dev/null); if [ "$n" = "k10temp" ]; then echo "== $h ($n)"; ls $h | sort; for f in $h/temp*_label $h/temp*_input; do printf "%s = %s\n" "$(basename $f)" "$(cat $f 2>/dev/null)"; done; fi; done; echo "---- sensors k10temp ----"; sensors 2>/dev/null | sed -n '/k10temp/,/^$/p'; echo "---- cpu ----"; grep -m1 "model name" /proc/cpuinfo; uname -r`
- (1x) `for g in 1 2 3; do echo "############ GPU $g ############"; nvidia-smi -q -i $g 2>&1 | sed -n '1,400p' | grep -A30 -E "^    (Clocks Event Reasons|Temperature|Power Readings|GPU Power Readings|Fan Speed|Performance State|PCI$)" | head -80; done`
- (1x) `for d in /sys/class/hwmon/hwmon*; do n=$(cat $d/name 2>/dev/null); if [ "$n" = "k10temp" ]; then echo "== $d ($n)"; ls $d; for f in $d/temp*_label $d/temp*_input; do printf "%s = %s\n" "$(basename $f)" "$(cat $f 2>/dev/null)"; done; fi; done; echo "---- sensors"; sensors 2>/dev/null | sed -n '/k10temp/,/^$/p'; echo "---- cpu"; grep -m1 "model name" /proc/cpuinfo; uname -r`
- (1x) `for d in /sys/class/hwmon/hwmon*; do n=$(cat $d/name 2>/dev/null); if [ "$n" = "k10temp" ]; then echo "== $d ($n)"; ls $d; for f in $d/temp*_label $d/temp*_input; do echo "$f = $(cat $f 2>/dev/null)"; done; fi; done; echo "---- sensors k10temp ----"; sensors 2>/dev/null | sed -n '/k10temp/,/^$/p'; echo "---- kernel ----"; uname -r; grep -m1 'model name' /proc/cpuinfo`
- (1x) `for d in /sys/class/hwmon/hwmon*; do n=$(cat $d/name 2>/dev/null); if [ "$n" = "k10temp" ]; then echo "== $d ($n)"; ls $d; for f in $d/temp*_label $d/temp*_input; do echo "$f = $(cat $f 2>/dev/null)"; done; fi; done; echo "---- sensors k10temp ----"; sensors 2>/dev/null | sed -n '/k10temp/,/^$/p'; echo "---- cpu ----"; grep -m1 "model name" /proc/cpuinfo; uname -r`
- (1x) `echo "═══ qui exécute nvidia-smi -c / compute mode ? ═══" ; grep -rlnE "nvidia-smi.*(-c |--compute-mode)" ~/jarvis /etc/systemd/system /etc/cron* ~/.config/systemd 2>/dev/null | head -10 ; echo "═══ occurrences précises ═══" ; grep -rhnE "nvidia-smi.*(-c [0-9]|--compute-mode)" ~/jarvis /etc/systemd/system ~/.config/systemd 2>/dev/null | head -8 | cut -c1-150`
- (1x) `echo "═══ qui charge GPU2 ? ═══" ; nvidia-smi --query-compute-apps=gpu_uuid,pid,used_memory,process_name --format=csv,noheader 2>/dev/null ; echo "--- UUID de GPU2 ---"; nvidia-smi --query-gpu=index,uuid,memory.used --format=csv,noheader 2>/dev/null | sed -n '3p'`
- (1x) `echo "── avant ──"; free -m | awk '/Mem:/{printf "  RAM %.0f%%\n",$3/$2*100}'; swapon --show=NAME,USED --noheadings | awk '{print "  swap "$1" "$2}' ; pkill -f "lm-studio" 2>/dev/null; pkill -f "llama.cpp-linux-x86_64-nvidia-cuda12-avx2.*llama-server" 2>/dev/null ; echo "arrêt demandé"`
- (1x) `echo "[ram-gpu-optimizer] FAST" ; free -m | awk '/Mem:/{printf "  RAM %.0f%% (%s/%s Mo)\n",$3/$2*100,$3,$2}' ; swapon --show=NAME,SIZE,USED,PRIO --noheadings 2>/dev/null | awk '{print "  swap "$1" "$3"/"$2" prio "$4}' ; ps aux --sort=-%mem 2>/dev/null | awk 'NR>=2&&NR<=5{printf "  %5.1f%% %6.0fMo %s\n",$4,$6/1024,$11}'`
- (1x) `echo "[gpu-ops] température et consommation" ; /usr/bin/nvidia-smi --query-gpu=index,name,temperature.gpu,utilization.gpu,power.draw,memory.used --format=csv,noheader ; echo "[gpu-ops] processus GPU" ; /usr/bin/nvidia-smi --query-compute-apps=pid,process_name,used_memory --format=csv,noheader 2>/dev/null | head -8`
- (1x) `echo "[gpu-ops] FAST" ; /usr/bin/nvidia-smi --query-gpu=index,temperature.gpu,memory.used,memory.total,power.draw --format=csv,noheader ; echo "[gpu-ops] throttle actif ?" ; /usr/bin/nvidia-smi --query-gpu=index,clocks_throttle_reasons.sw_thermal_slowdown --format=csv,noheader | grep -i active || echo "  aucun throttle en cours"`
- (1x) `echo "[gpu-ops] FAST" ; /usr/bin/nvidia-smi --query-gpu=index,temperature.gpu,clocks_throttle_reasons.sw_thermal_slowdown,power.draw,utilization.gpu --format=csv,noheader ; echo "[gpu-ops] qui consomme le GPU maintenant" ; /usr/bin/nvidia-smi --query-compute-apps=pid,used_memory --format=csv,noheader | awk -F', ' '{s[$1]+=$2} END{for(p in s) print p, s[p]" MiB"}' | sort -k2 -rn | head -4`
- (1x) `echo "S3 · bon code, opération neutre (-pl 70 sur GPU1 déjà à 70W)" ; JARVIS_GPU_UNLOCK=1202 nvidia-smi -i 1 -pl 70 </dev/null 2>&1 | tail -2; echo "   RC=${PIPESTATUS[0]} (0 attendu)"`
- (1x) `echo "=== échantillonnage GPU 20s (1/s) ==="; for i in $(seq 1 20); do nvidia-smi --query-gpu=index,temperature.gpu,utilization.gpu,memory.used,power.draw --format=csv,noheader,nounits | paste -sd' | '; sleep 1; done`
- (1x) `echo "=== swapon ==="; swapon --show; echo "=== zramctl ==="; zramctl 2>/dev/null; echo "=== /proc/swaps ==="; cat /proc/swaps`
- (1x) `echo "=== sensors ==="; sensors 2>&1 | head -80; echo; echo "=== lsmod nct ==="; lsmod 2>/dev/null | grep -iE "nct|it87|coretemp|k10temp"; echo; echo "=== hwmon ==="; for d in /sys/class/hwmon/hwmon*; do echo "$d: $(cat $d/name 2>/dev/null)"; done`
- (1x) `echo "=== sdc1 état ==="; sudo dumpe2fs -h /dev/sdc1 2>&1 | grep -E "Filesystem state|Last checked|Filesystem UUID|Last mounted"  ; echo "=== disques présents ==="; lsblk -o NAME,SIZE,MODEL,SERIAL -d 2>&1 | head ; echo "=== by-id sdc ==="; ls -l /dev/disk/by-id/ 2>/dev/null | grep -E "sdc$|sda$"`
- (1x) `echo "=== quels GPU sont des 1660 SUPER + plage de power limit ===" ; nvidia-smi --query-gpu=index,name,power.limit,power.min_limit,power.max_limit --format=csv,noheader 2>/dev/null | sed 's/^/  /'`
- (1x) `echo "=== processus tenant la VRAM ==="; nvidia-smi --query-compute-apps=pid,used_memory,name --format=csv 2>/dev/null ; echo "=== fallback ==="; sudo fuser -v /dev/nvidia0 2>&1 | head -10 ; echo "=== llama-server orphelins ==="; pgrep -af 'llama-server' | head -5`
- (1x) `echo "=== memoire / swap ==="; free -h | head -3 ; echo "=== priorites swap (zram doit dominer) ==="; swapon --show 2>/dev/null ; echo "=== espace disque ==="; df -h / /boot/efi /mnt/jarvis-data /mnt/nvme-fast 2>/dev/null | grep -v tmpfs`
- (1x) `echo "=== fan% (croisement avec nvidia-smi: 2060=59% 3080=53% 1660S=0%) ===" ; paste <(DISPLAY=:0 nvidia-settings -q GPUCurrentFanSpeed -t 2>/dev/null) <(DISPLAY=:0 nvidia-settings -q GPUCurrentFanSpeedRPM -t 2>/dev/null) | nl -v0 | awk '{printf "fan:%s  duty=%s%%  rpm=%s\n",$1,$2,$3}'`
- (1x) `echo "=== espace disponible ailleurs ==="; df -h /mnt/nvme-fast /mnt/backup-os / 2>&1 ; echo "--- sda1 (456G, non monté ?) ---"; findmnt /dev/sda1 2>&1 || echo "sda1 NON monté"`
- (1x) `echo "=== env CUDA du llama-server 1957604 ==="; tr '\0' '\n' < /proc/1957604/environ 2>/dev/null | grep -iE 'CUDA|VISIBLE|GPU|HIP' || echo "(aucune var CUDA_VISIBLE_DEVICES -> tous les GPU visibles, index CUDA == index nvidia-smi)"; echo; echo "=== env CUDA du process LM Studio principal 426883 ==="; tr '\0' '\n' < /proc/426883/environ 2>/dev/null | grep -iE 'CUDA|VISIBLE|GPU' || echo "(aucune)"`
- (1x) `echo "=== diskstats nvme0n1 ==="; grep -E 'nvme0n1|zram0' /proc/diskstats ; echo "=== uptime ==="; cat /proc/uptime ; echo "=== swappiness ==="; cat /proc/sys/vm/swappiness ; echo "=== mount info ==="; findmnt /mnt/nvme-fast; lsblk -o NAME,SIZE,FSTYPE,LABEL,MOUNTPOINT /dev/nvme0n1`
- (1x) `echo "=== confirmation : le driver refuse-t-il 60W ? (test réel) ===" ; sudo nvidia-smi -i 2 -pl 60 2>&1 | head -2 | sed 's/^/  /' ; echo "=== état thermique actuel des 4 GPU (tolérance 100°C) ===" ; nvidia-smi --query-gpu=index,name,temperature.gpu,power.limit,power.draw,utilization.gpu --format=csv,noheader 2>/dev/null | sed 's/^/  /'`
- (1x) `echo "=== blkid sda1 ==="; sudo -n blkid /dev/disk/by-id/ata-WD_Blue_SA510_2.5_1000GB_24375P800971-part1 2>&1 || blkid /dev/sda1 2>&1; echo; echo "=== lsblk -f sda ==="; lsblk -f /dev/sda 2>&1; echo; echo "=== partition table sda (read-only) ==="; sudo -n sfdisk -l /dev/disk/by-id/ata-WD_Blue_SA510_2.5_1000GB_24375P800971 2>&1 | head -30`
- (1x) `echo "=== bench en cours ==="; wc -l /home/turbo/jarvis/artifacts/e1-20260806/bench-post-fix.tsv 2>/dev/null ; echo "=== charge / GPU sous 16 flux ==="; uptime | grep -oE "load average.*"; /usr/bin/nvidia-smi --query-gpu=index,utilization.gpu,memory.used,temperature.gpu --format=csv,noheader ; echo "=== processus de fond ==="; pgrep -cf "jarvis|python3"`
- (1x) `echo "=== arbre PCIe ==="; lspci -tv 2>/dev/null | head -40 ; echo; echo "=== tous devices NVIDIA (meme non-VGA) ==="; lspci -d 10de:  ; echo; echo "=== slots PCIe et link status ==="; sudo lspci -vv -d 10de: 2>/dev/null | grep -E '^[0-9a-f]{2}:|LnkSta:|LnkCap:' | head -40`
- (1x) `echo "=== [2] EFI / GRUB ==="; sudo efibootmgr -v 2>&1 | head -20 ; echo "--- ESP ---"; findmnt /boot/efi 2>&1; df -h /boot /boot/efi / 2>/dev/null ; echo "--- grub cfg ---"; ls -la /boot/grub/grub.cfg 2>&1; grep -c menuentry /boot/grub/grub.cfg 2>/dev/null ; echo "--- kernels installés ---"; ls -la /boot/vmlinuz-* /boot/initrd.img-* 2>&1 ; echo "--- kernel courant ---"; uname -r; uname -a`
- (1x) `echo "=== VENTILATEURS (RPM) ==="; sensors -A 2>/dev/null | grep -iE "fan|rpm|pwm" ; echo; echo "=== TOUS CAPTEURS BRUTS ==="; sensors -A 2>/dev/null | head -60; echo; echo "=== CHARGE ==="; uptime; echo; echo "=== TOP CPU ==="; ps -eo pcpu,pid,comm --sort=-pcpu | head -12`
- (1x) `echo "=== UPTIME/LOAD ==="; uptime; echo; echo "=== MEM ==="; free -h; echo; echo "=== SWAP DETAIL ==="; swapon --show 2>/dev/null || echo "(swapon indisponible)"; echo; echo "=== DF ==="; df -h -x tmpfs -x devtmpfs 2>/dev/null; echo; echo "=== ZOMBIES ==="; ps -eo stat= | grep -c '^Z' ; echo "--- liste zombies ---"; ps -eo pid,ppid,stat,comm --sort=stat | awk '$3 ~ /^Z/' | head -20`
- (1x) `echo "=== TICK manuel (5 tâches) ===" ; python3 ~/jarvis/bin/jarvis-executor.py --tick 5 2>&1 | tail -12 ; echo "" ; echo "=== état file après ===" ; python3 ~/jarvis/bin/jarvis-executor.py --status 2>&1 | head -3 ; echo "=== surveillance ===" ; nvidia-smi --query-gpu=temperature.gpu --format=csv,noheader 2>/dev/null|tr '\n' ' '; uptime|grep -oE "load average.*"`
- (1x) `echo "=== TEST LECTURE (doit passer) ===" ; nvidia-smi --query-gpu=index,name --format=csv,noheader 2>&1 | head -6 ; echo "RC=$?" ; echo "=== TEST ECRITURE sans code (doit REFUSER) ===" ; nvidia-smi -i 2 -c PROHIBITED < /dev/null 2>&1 | tail -3; echo "RC=${PIPESTATUS[0]}"`
- (1x) `echo "=== TEMPÉRATURES ACTUELLES ==="; for d in /sys/class/hwmon/hwmon*; do n=$(cat $d/name 2>/dev/null); for t in $d/temp*_input; do [ -f "$t" ] && echo "$n $(basename $t): $(( $(cat $t) / 1000 ))°C"; done; done 2>/dev/null; echo; echo "=== GPU ==="; nvidia-smi --query-gpu=index,name,temperature.gpu,fan.speed,power.draw --format=csv 2>/dev/null; echo; echo "=== LOAD ==="; uptime`
- (1x) `echo "=== TEMPS CPU/CARTE ==="; sensors 2>/dev/null | grep -iE "tctl|tccd|composite|Core|fan|°C" | head -20; echo "=== LOAD evol ==="; cat /proc/loadavg; echo "=== pressure ==="; for f in cpu io memory; do echo "$f: $(head -1 /proc/pressure/$f 2>/dev/null)"; done`
- (1x) `echo "=== Secure Boot ==="; mokutil --sb-state 2>&1; echo; echo "=== modules NVIDIA chargés ==="; lsmod 2>/dev/null | grep -c nvidia; nvidia-smi --query-gpu=name --format=csv,noheader 2>&1 | head -5; echo; echo "=== noyau courant ==="; uname -r`
- (1x) `echo "=== SSD RECUPERE : sdh (SN 24375P800237) ==="; lsblk -o NAME,SIZE,FSTYPE,LABEL,UUID,MOUNTPOINT /dev/sdh 2>&1; echo; echo "=== SMART ==="; sudo smartctl -H -A /dev/sdh 2>/dev/null | grep -iE 'overall-health|Power_On_Hours|Reallocated|CRC|Wearout|Temperature'; echo; echo "=== PORT ata2 (5e SSD toujours absent) ==="; dmesg 2>/dev/null | grep -E 'ata2' | tail -3`
- (1x) `echo "=== SDD SMART ==="; lsblk -o NAME,SIZE,MODEL,MOUNTPOINT /dev/sdd 2>/dev/null; sudo -n smartctl -H -A /dev/sdd 2>/dev/null | grep -iE "result|Reallocated|Pending|Uncorrect|CRC|Power_On" || echo "(smartctl sudo indisponible)" ; echo; echo "=== CRON toutes les X min ==="; crontab -l 2>/dev/null | grep -vE "^#" | head -30`
- (1x) `echo "=== RPM réel (tach) via nvidia-settings ===" ; DISPLAY=:0 nvidia-settings -q GPUCurrentFanSpeedRPM -t 2>&1 | head -12 ; echo "--- fans détectés ---" ; DISPLAY=:0 nvidia-settings -q fans -t 2>&1 | head -12 ; echo "=== nvidia-smi -q -d FAN ===" ; nvidia-smi -q 2>&1 | grep -E "^GPU 0000|Fan Speed" | sed 's/^ *//'`
- (1x) `echo "=== PROCESSUS SUR /mnt/jarvis-data ==="; sudo lsof +D /mnt/jarvis-data 2>/dev/null | head -10; sudo fuser -vm /mnt/jarvis-data 2>&1 | head -10; echo "(vide = personne)"; echo; echo "=== PARTITIONS DU DISQUE (maintenant sdg) ==="; lsblk -o NAME,SIZE,LABEL,UUID,MOUNTPOINT /dev/sdg 2>&1`
- (1x) `echo "=== PARTUUID des partitions EFI ==="; lsblk -o NAME,SIZE,PARTUUID,PARTTYPENAME,FSTYPE,LABEL,MOUNTPOINT /dev/sda /dev/sdb /dev/sdc 2>/dev/null ; echo; echo "=== entree de boot REELLEMENT utilisee (BootCurrent 001B) ==="; sudo efibootmgr -v 2>/dev/null | grep -E 'Boot001[AB]|Boot001B' ; echo; echo "=== UUID dupliques ? ==="; sudo blkid 2>/dev/null | grep -E '/dev/sd[abc]'`
- (1x) `echo "=== PARTUUID avant ==="; lsblk -no NAME,PARTUUID /dev/sdc ; sudo sgdisk --partition-guid=1:R --partition-guid=2:R /dev/sdc 2>&1 | tail -3 ; sudo partprobe /dev/sdc 2>&1 | head -2 ; echo "=== PARTUUID apres ==="; lsblk -no NAME,PARTUUID,LABEL /dev/sdc`
- (1x) `echo "=== PARTUUID ==="; lsblk -o NAME,PARTUUID,LABEL,SIZE,FSTYPE /dev/sda /dev/sdb /dev/sdc 2>&1; echo; echo "=== ESP sdb1 (monté /boot/efi) ==="; ls -R /boot/efi/EFI 2>&1 | head -40`
- (1x) `echo "=== PARTUUID (table GPT — deja corriges, lecture seule) ===" ; lsblk -o NAME,SIZE,PARTUUID,LABEL /dev/sda /dev/sdc 2>/dev/null ; echo ; echo "=== PARTUUID encore en double sur la machine ? ===" ; lsblk -no PARTUUID 2>/dev/null | grep -v '^$' | sort | uniq -d | head || true ; echo "  (aucune ligne ci-dessus = plus aucun doublon de PARTUUID)"`
- (1x) `echo "=== OU EST LE NVMe (partage M.2/SATA ?) ==="; lspci -nn | grep -i 'non-volatile\|nvme'; echo; echo "=== SLOTS PHYSIQUES DECLARES PAR LE BIOS ==="; sudo dmidecode -t slot 2>/dev/null | grep -E 'Designation|Current Usage|Type:|Bus Address' | paste - - - - 2>/dev/null | head -12`
- (1x) `echo "=== NVIDIA-SMI ==="; nvidia-smi --query-gpu=index,name,memory.total,memory.used,temperature.gpu,utilization.gpu --format=csv 2>&1 || echo "ECHEC nvidia-smi"; echo; echo "=== DRIVER ==="; nvidia-smi --query-gpu=driver_version --format=csv,noheader 2>&1 | head -1`
- (1x) `echo "=== NVIDIA MAINTENANT ==="; nvidia-smi 2>&1 | head -12; echo; echo "=== ERREUR GRUB/SHIM ==="; sudo -n dpkg --configure grub-efi-amd64-signed 2>&1 | tail -25`
- (1x) `echo "=== NVIDIA DRM MODESET ==="; cat /sys/module/nvidia_drm/parameters/modeset 2>&1; echo "fbdev:"; cat /sys/module/nvidia_drm/parameters/fbdev 2>&1; echo; echo "=== OPTIONS MODULE ==="; grep -rhE 'nvidia' /etc/modprobe.d/*.conf 2>/dev/null | grep -v '^#' | head -10; echo; echo "=== DRIVER NVIDIA ==="; nvidia-smi --query-gpu=driver_version --format=csv,noheader | head -1`
- (1x) `echo "=== MODULE NCT6687 (capteurs B550-A PRO) ==="; lsmod | grep -i nct; echo "dkms:"; dkms status 2>/dev/null | grep -i nct; echo "module dispo:"; modinfo nct6687 2>&1 | grep -E '^filename|^version' ; echo; echo "=== CAPTEURS ACTUELS ==="; sensors 2>/dev/null | head -30`
- (1x) `echo "=== MODULE CAPTEURS VENTILO ==="; sudo modprobe nct6775 2>&1; sudo modprobe nct6683 2>&1; sleep 2; sensors -A 2>/dev/null | grep -iA2 "fan\|nct" | head -30; echo; echo "=== MODULES CHARGES ==="; lsmod | grep -iE "nct|it87|coretemp|k10temp"`
- (1x) `echo "=== LABELS BRUTS DU CAPTEUR ==="; sensors -u nct6687-isa-0a20 2>/dev/null | head -20`
- (1x) `echo "=== INODES ==="; df -i / /home /mnt/jarvis-data /mnt/nvme-fast /boot/efi 2>&1 ; echo; echo "=== espace réel ==="; df -h / 2>&1 ; echo; echo "=== /tmp /var/tmp /dev/shm ==="; df -ih /tmp /var/tmp /dev/shm 2>&1`
- (1x) `echo "=== GRUB CONF ==="; grep -vE '^\s*#|^\s*$' /etc/default/grub; echo; echo "=== SECURE BOOT ==="; mokutil --sb-state 2>&1; echo; echo "=== GPU ==="; nvidia-smi --query-gpu=index,name,pci.bus_id,temperature.gpu,power.draw,compute_mode --format=csv,noheader 2>&1; echo; echo "=== GPU PCI COUNT ==="; lspci | grep -ci vga; lspci | grep -i 'vga\|3d'`
- (1x) `echo "=== GPU0 = ecran ? (piege famine VRAM) ==="; nvidia-smi --query-compute-apps=gpu_uuid,pid,used_memory,process_name --format=csv,noheader 2>/dev/null | head ; echo; echo "--- quel GPU pilote l'affichage ---"; nvidia-smi --query-gpu=index,name,display_active,display_mode --format=csv,noheader 2>/dev/null`
- (1x) `echo "=== GPU d'affichage (Xorg) ==="; nvidia-smi --query-compute-apps=pid,gpu_uuid,used_memory --format=csv 2>&1; nvidia-smi | grep -Ei 'Xorg|gnome|process|MiB' | head -20; echo "=== sudo -n ? ==="; sudo -n true 2>&1 && echo SUDO_OK || echo SUDO_KO`
- (1x) `echo "=== GPU THERMAL DETAIL ==="; nvidia-smi --query-gpu=index,name,temperature.gpu,power.draw,power.limit,clocks.sm,clocks_throttle_reasons.active,utilization.gpu,memory.used,memory.total --format=csv,noheader 2>/dev/null; echo; echo "=== THROTTLE GPU3 ==="; nvidia-smi -i 3 -q -d PERFORMANCE 2>/dev/null | grep -A12 "Clocks Event Reasons" | head -16`
- (1x) `echo "=== GPU ===" ; nvidia-smi --query-gpu=index,name,temperature.gpu,utilization.gpu,memory.used,power.draw --format=csv,noheader 2>&1 ; echo ; echo "=== CPU ===" ; sensors 2>/dev/null | grep -iE "Tctl|Tccd|CPU|fan" | head -10`
- (1x) `echo "=== FSCK : avancement ==="; pgrep e2fsck >/dev/null && { echo "EN COURS depuis $(ps -o etime= -p $(pgrep e2fsck) | tr -d ' ')"; tail -2 /home/turbo/jarvis/backups/boot-repair-20260806/e2fsck-backupos-run2.log | head -1 | cut -c1-90; } || echo TERMINE ; echo; echo "=== CHARGE ACTUELLE ==="; uptime | sed 's/.*load/load/'; free -m | awk 'NR==2{printf "RAM %s/%s Mo\n",$3,$2}'`
- (1x) `echo "=== EFI BOOT ORDER ==="; sudo efibootmgr -v 2>/dev/null || efibootmgr -v 2>/dev/null; echo; echo "=== DISQUES ==="; lsblk -o NAME,SIZE,TYPE,FSTYPE,PARTUUID,UUID,MOUNTPOINT 2>/dev/null | head -50`
- (1x) `echo "=== EFI BOOT ORDER ==="; efibootmgr -v 2>&1 | head -30; echo; echo "=== ESP MOUNT ==="; findmnt /boot/efi 2>&1; lsblk -o NAME,SIZE,FSTYPE,PARTTYPENAME,MOUNTPOINT,LABEL 2>&1 | head -40`
- (1x) `echo "=== EFI ==="; efibootmgr 2>/dev/null | head -15; echo "=== /boot ==="; ls -la /boot/ 2>/dev/null | head -15; df -h /boot /boot/efi 2>/dev/null; echo "=== GRUB default ==="; grep -E "^GRUB_(DEFAULT|TIMEOUT|CMDLINE_LINUX_DEFAULT|RECORDFAIL)" /etc/default/grub 2>/dev/null; echo "=== Kernels ==="; ls -la /boot/vmlinuz* /boot/initrd* 2>/dev/null`
- (1x) `echo "=== DISQUES ==="; lsblk -o NAME,SIZE,TYPE,FSTYPE,MOUNTPOINT,LABEL 2>/dev/null | head -30 ; echo; echo "=== PARTITION ROOT ACTUELLE ==="; findmnt -n / ; findmnt -n /boot/efi 2>/dev/null || echo "  ⚠ /boot/efi NON MONTE" ; echo; echo "=== ENTREES EFI (ordre de boot) ==="; sudo efibootmgr -v 2>/dev/null | head -20`
- (1x) `echo "=== CHARGE / THERMIQUE (11 agents actifs) ==="; uptime|sed 's/.*load average: /load: /'; sensors nct6687-isa-0a20 2>/dev/null | grep -E '^CPU:|^VRM'; free -m|awk 'NR==2{printf "RAM %s/%s (%.0f%%)\n",$3,$2,$3*100/$2}' ; echo; nvidia-smi --query-gpu=index,temperature.gpu --format=csv,noheader 2>/dev/null|tr '\n' ' '`
- (1x) `echo "=== CAPTEURS : etat du watercooling ==="; sensors nct6687-isa-0a20 2>/dev/null | grep -E 'Pump|CPU Fan|System Fan|CPU:|\+5V|\+12V' ; echo; echo "=== le rail 5V alimente l'ARGB 3 pins ==="; sensors -u nct6687-isa-0a20 2>/dev/null | awk '/^in1_input/{printf "  +5V actuel : %.3f V\n",$2} /^in1_min/{printf "  +5V minimum: %.3f V  (tolerance ATX : 4.75 - 5.25)\n",$2}'`
- (1x) `echo "=== BLOCK DEVICES ==="; lsblk -o NAME,SIZE,TYPE,FSTYPE,LABEL,UUID,PARTUUID,MOUNTPOINT,SERIAL 2>/dev/null; echo; echo "=== SERIALS ==="; lsblk -d -o NAME,SIZE,SERIAL,MODEL 2>/dev/null`
- (1x) `echo "=== 07:00.0 present ? ==="; ls -d /sys/bus/pci/devices/0000:07:00.0 2>&1; lspci -s 07:00.0 2>&1; echo; echo "=== TOUS LES PONTS ET GPU PCI ==="; lspci -tv 2>/dev/null | grep -iE 'nvidia|bridge' | head -20; echo; echo "=== LOG WATCHDOG (fantome) ==="; grep -i 'fant' /var/log/jarvis-watchdog.log 2>/dev/null | tail -5 || sudo grep -ri 'fant' /var/log/jarvis*watchdog* 2>/dev/null | tail -5`
- (1x) `echo "=== /mnt/backup-os monte ? ==="; findmnt -n -o TARGET,SOURCE /mnt/backup-os 2>&1 || echo "NON MONTE"; echo; echo "=== e2fsck en cours ? ==="; pgrep -a e2fsck 2>&1 || echo "termine"; echo; echo "=== sdg1 utilise par qui ==="; lsblk -o NAME,MOUNTPOINT /dev/sdg 2>&1`
- (1x) `echo "== M1 BLOCK =="; lsblk -o NAME,SIZE,TYPE,FSTYPE,MOUNTPOINT,MODEL,SERIAL 2>/dev/null; echo; echo "== M1 DF =="; df -h -x tmpfs -x devtmpfs | head -20`
- (1x) `echo "--- proc lm-studio ---"; ps aux | grep -E "lm-studio|llama-server" | grep -v grep | awk '{print $2,$3,$4,$8,substr($0,index($0,$11),120)}' | head -10 ; echo "--- port ---"; ss -ltn | grep 1234 || echo "AUCUN listener 1234" ; echo "--- dmesg oom ---"; dmesg -T 2>/dev/null | grep -iE "oom|killed process" | tail -8 ; echo "--- mem ---"; free -g | head -3`
- (1x) `echo "--- avant : $(lspci -d 10de: | grep -c 'VGA') GPU ---" ; sudo sh -c 'echo 1 > /sys/bus/pci/rescan' ; sleep 3 ; echo "--- apres rescan : $(lspci -d 10de: | grep -c 'VGA') GPU ---" ; lspci -d 10de: | grep VGA ; echo "--- nvidia-smi ---"; nvidia-smi --query-gpu=index,name,pci.bus_id --format=csv`
- (1x) `echo "### N4 — ponderation thermique GPU ###" ; nvidia-smi --query-gpu=index,name,temperature.gpu,memory.total,memory.used --format=csv,noheader 2>&1 | head -8 ; echo "### VRAM totale M1 ###"; nvidia-smi --query-gpu=memory.total --format=csv,noheader 2>/dev/null | awk '{s+=$1} END{print "  cumul = "s" MiB"}'`
- (1x) `df -hT -x tmpfs -x devtmpfs 2>/dev/null; echo "--- INODES ---"; df -i -x tmpfs -x devtmpfs 2>/dev/null | awk 'NR==1||$5+0>80'`
- (1x) `df -h /mnt/nvme-fast; echo "--- DU ---"; du -sh /mnt/nvme-fast/* 2>/dev/null | sort -rh | head -15`
- (1x) `df -h /mnt/nvme-fast 2>/dev/null; ls -la /mnt/nvme-fast/jarvis/recherche/ 2>/dev/null || echo "dossier absent"; ls -la /home/turbo/jarvis/board/recherche/ 2>/dev/null || echo "board/recherche absent"`
- (1x) `df -h / /mnt/jarvis-data 2>/dev/null | tail -2`
- (1x) `df -h / /home /tmp /var /mnt/jarvis-data 2>/dev/null | grep -v '^Sys'; echo "──"; grep -n 'REGISTRY' /home/turbo/jarvis/bin/reports-reindex.py | head -3`
- (1x) `df -h / /boot /var /tmp /home 2>/dev/null | grep -vE '^tmpfs|^udev'; echo "--- inodes ---"; df -i / | tail -1`
- (1x) `df -h / && echo "---" && find /home/turbo -name __pycache__ -type d 2>/dev/null | wc -l`
- (1x) `df -h / && echo "---" && df / | awk '{if(NR==2) {pct=$(5); gsub("%", "", pct); if(pct+0 <= 85) print "✓ OBJECTIF ATTEINT: " pct "% < 85%"; else print "⚠ SEUIL: " pct "%"}}'`
- (1x) `df -h -x tmpfs -x devtmpfs -x squashfs 2>/dev/null`
- (1x) `cd ~/jarvis/bin && timeout 200 python3 notebook-local.py --etat 2>&1 | tail -7 ; echo; echo "=== GPU apres bascule ==="; nvidia-smi --query-gpu=index,name,memory.used,temperature.gpu,utilization.gpu --format=csv,noheader 2>/dev/null`
- (1x) `cd /tmp && gh api repos/NVIDIA/k8s-device-plugin/issues/941/comments --jq '.[] | "=== \(.user.login) \(.created_at) ===\n\(.body)"' 2>&1 | grep -inE "k8s at all|kubernetes|reddit|535\.|550\.|driver version|GPU|4x|nvidia-smi|4 GPU|reddit|developer forum" | head -60`
- (1x) `cat /usr/local/bin/nvidia-smi`
- (1x) `cat /proc/loadavg ; free -h ; echo "---" ; python3 ~/jarvis/bin/skillmp-pipeline.py status`
- (1x) `cat /proc/257604/cmdline | tr '\0' ' ' | grep -oE '\-\-(main-gpu|tensor-split|split-mode) [^ ]+'; echo '--- 240187 vivant ?'; ps -p 240187 -o pid= 2>/dev/null || echo mort; echo '--- alloc actuelle ---'; nvidia-smi --query-compute-apps=gpu_bus_id,pid,used_memory --format=csv,noheader | grep -E '240187|257604'; nvidia-smi --query-gpu=index,temperature.gpu --format=csv,noheader`
- (1x) `/usr/bin/nvidia-smi --query-gpu=index,name,power.limit,power.min_limit,power.max_limit,compute_mode --format=csv 2>&1; echo "---MIG/CC/ACP dans le help ?---"; /usr/bin/nvidia-smi --help 2>&1 | grep -Ei '\-(mig|acp|cc|gtt|dm)\b|multi-instance|applications-clocks-permission|conf-compute|target-temp|driver-model'`
- (1x) `/usr/bin/nvidia-smi --query-gpu=index,name,memory.total,uuid --format=csv 2>&1`
- (1x) `/usr/bin/nvidia-smi --query-gpu=index,name,memory.total,memory.used,temperature.gpu,utilization.gpu,power.draw,power.limit --format=csv 2>&1`
