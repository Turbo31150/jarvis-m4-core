# Commandes M1 — docker-conteneur

## A QUOI CA SERT

Conteneurs et orchestration. Lister ce qui tourne, lire les journaux d'un conteneur, verifier un service Swarm.

## COMMANDES REELLEMENT EXECUTEES

Extraites de 1840 sessions Claude Code sur M1, triees par frequence.
Un chemin en /home/turbo ou une IP 192.168.0.10 doit etre reecrit avant rejeu sur M4.

- (2x) `cd /home/turbo/jarvis ; python3 -c "import ast;ast.parse(open('bin/atomiser-logiques.py').read());print('syntaxe OK')" ; for i in "construire une image docker" "installer les dependances python" "deployer une app node avec docker"; do ;   echo "=== $i ===" ;   timeout 60 python3 bin/atomiser-logiques.py reconstruire "$i" --pas 4 2>&1 | grep -E '^\s+[0-9]\.' | head -4 ; done`
- (1x) `timeout 5 docker ps -a --format '{{.Names}}\t{{.Status}}' 2>&1 | head -60`
- (1x) `timeout 5 docker ps -a --format '{{.Names}}' 2>&1 | grep -iE 'openclaw|cowork|omega|agent' | sort | head -30; echo "=== RUNNING SEUL ==="; timeout 5 docker ps --format '{{.Names}}\t{{.Status}}' 2>&1 | head -30`
- (1x) `ss -tlnp 2>/dev/null | grep -E ':3389' ; sudo -n ss -tlnp 2>/dev/null | grep 3389; echo "==="; docker ps --format '{{.Names}}\t{{.Ports}}' | grep 3389`
- (1x) `ls -t ~/jarvis/audit/*.md 2>/dev/null | head -3; grep -n -i -E 'docker|service|container' /home/turbo/jarvis/bin/jarvis-planning-widget.py | head -6`
- (1x) `ls -la /home/turbo/jarvis-linux/infra/docker/docker-compose.modules.yml 2>&1; grep -n 'container_name' /home/turbo/jarvis-linux/infra/docker/docker-compose.modules.yml 2>/dev/null | head -20`
- (1x) `ls -la /home/turbo/Bureau/artifacts/ | tail -10 ; echo "=== fichiers du CDC retrouves ===" ; for f in deploy-jarvis-ecosystem.sh jarvis-ecosystem-docker-compose.yml openclaw-agents-config.yaml jarvis-ecosystem-todo.json; do ;   p=/home/turbo/Bureau/artifacts/$f ;   [ -f "$p" ] && echo "  TROUVÉ  $f ($(du -h "$p" | cut -f1))" || echo "  absent  $f" ; done`
- (1x) `grep -rl "jv-studio-s-alkymia-s" /home/turbo/alkymia --include='*.sh' --include='*.yml' --include='*.yaml' --include='*.json' 2>/dev/null | head; echo "==="; ls /home/turbo/alkymia/docker-compose.yml /home/turbo/alkymia/compose.yml 2>/dev/null; docker images --format '{{.Repository}}:{{.Tag}}' | grep -iE 'alkym|studio' | head -5`
- (1x) `grep -A5 '^networks:' /home/turbo/alkymia/docker-compose.yml; echo "==="; docker network ls | grep -i alkym`
- (1x) `for q in "migrer pile docker" "ponts services" "cascade llm" "classer depots"; do echo "=== bloc.sh $q ==="; timeout 30 ~/jarvis/bin/bloc.sh $q 2>&1 | head -5; echo; done`
- (1x) `for d in /home/turbo/jarvis-zero-token /home/turbo/jarvis-cowork /home/turbo/jarvis-machines-private /home/turbo/alkymia; do ;   echo "##### $d/AUTOMATION.md" ;   grep -iE "^#|run|lancer|démarr|start|exec|python3|bash|npm|docker" "$d/AUTOMATION.md" 2>/dev/null | head -12 ; done`
- (1x) `for c in jarvis_prod_postgres.1.o5tuz7jcvl9v32hlotgaw4xbc jv-infra-biblio-db; do echo "=== $c"; docker exec "$c" env 2>/dev/null | grep -iE '^POSTGRES_(USER|DB)='; done`
- (1x) `for C in jv-infra-biblio-db $(docker ps --format '{{.Names}}' | grep jarvis_prod_postgres); do ;   U=$(docker inspect "$C" --format '{{range .Config.Env}}{{println .}}{{end}}' 2>/dev/null | grep -E '^POSTGRES_(USER|DB)=' | tr '\n' ' ') ;   echo "═══ $C  [$U] ═══" ; done`
- (1x) `echo "════ ré-exécution réelle (détection de refus active) ════" ; timeout 580 python3 ~/jarvis/bin/jarvis-dag.py --run "surveille les alertes docker et préviens sur telegram" 2>&1 | tail -14`
- (1x) `echo "=== taches jarvis_prod EN COURS ===" ; docker ps --filter 'name=jarvis_prod' --format '  {{.Names}}  {{.Status}}' 2>/dev/null | head -10 ; echo "  (vide = aucune tache active)" ; echo; echo "=== services Swarm ===" ; docker service ls --format '  {{.Name}}  replicas={{.Replicas}}  {{.Image}}' 2>/dev/null | head -12`
- (1x) `echo "=== nginx.conf autour de 134 ==="; sed -n '110,160p' /home/turbo/Workspaces/jarvis-linux/infra/docker/nginx.conf | cat -n | sed 's/^/  /' ; echo; echo "=== toutes les occurrences antigravity/8902 ==="; grep -n 'antigravity\|8902\|18902' /home/turbo/Workspaces/jarvis-linux/infra/docker/nginx.conf`
- (1x) `echo "=== docker cmdlib ==="; docker ps -a --format '{{.Names}}\t{{.Status}}\t{{.Image}}' 2>&1 | grep -i -E 'commande|cmdlib|biblio' ; echo "--- all running count ---"; docker ps -q 2>&1 | wc -l`
- (1x) `echo "=== conteneurs créés depuis le boot ==="; docker ps -a --format '{{.CreatedAt}}\t{{.Image}}\t{{.Names}}\t{{.Command}}' 2>&1 | sort -r | head -15; echo; echo "=== TAUX actuel: events docker 60s ==="; timeout 62 docker events --since 60s --until 1s --filter event=create --format '{{.Actor.Attributes.image}}' 2>/dev/null | sort | uniq -c | sort -rn | head`
- (1x) `echo "=== conteneur openclaw-node running ==="; C=$(docker ps --format '{{.Names}}' | grep openclaw-node | head -1); echo "$C"; docker exec "$C" sh -c 'ls /app 2>/dev/null | head -20; echo "--- agents dir ---"; ls -d /root/.openclaw 2>/dev/null && ls /root/.openclaw | head; echo "--- proc ---"; ps aux | head -15' 2>&1 | head -50`
- (1x) `echo "=== container hôte du domino_executor ==="; docker ps --format '{{.Names}}\t{{.Status}}\t{{.Image}}' 2>&1 | grep -i "domino\|ops\|automation" ; echo "--- tous ---"; docker ps --format '{{.Names}}\t{{.Status}}' 2>&1 | head -25`
- (1x) `echo "=== container biblio-db (nom reel) ==="; docker ps -a --format '{{.Names}}' 2>&1 | grep -i -E 'biblio|jv-infra' ; echo "(vide = absent)"; echo "=== images biblio ==="; docker images --format '{{.Repository}}:{{.Tag}}' 2>&1 | grep -i -E 'biblio|cmdlib|postgres'; echo "=== volumes ==="; docker volume ls 2>&1 | grep -i -E 'biblio|cmdlib'`
- (1x) `echo "=== confirmation swarm LOCAL M1 (jarvis_prod_*) non touché ===" ; docker service ls 2>&1 | grep jarvis_prod || echo "docker local M1 introuvable via docker natif ou aucun service jarvis_prod (vérification informative uniquement, pas d'action requise)"`
- (1x) `echo "=== bloc.sh ==="; ls -la /home/turbo/jarvis/bin/bloc.sh 2>&1; echo "--- which bloc ---"; which bloc bloc.sh 2>&1; echo "--- test lecture ---"; timeout 30 bash /home/turbo/jarvis/bin/bloc.sh docker 2>&1 | head -20; echo "EXIT=$?"`
- (1x) `echo "=== TOTAL CONTENEURS ==="; docker ps -a -q | wc -l; echo "=== openclaw-sbx ==="; docker ps -a --format '{{.Names}}\t{{.State}}\t{{.Image}}' | grep -i openclaw || echo "AUCUN conteneur openclaw-sbx-agent-*"; echo "=== IMAGES ==="; docker images --format '{{.Repository}}:{{.Tag}}' | head -40`
- (1x) `echo "=== SWARM M1 ==="; docker info 2>/dev/null | grep -A6 -i "swarm:" | head -10; echo "--- nodes ---"; docker node ls 2>&1 | head; echo "--- stacks ---"; docker stack ls 2>&1; echo "--- services ---"; docker service ls 2>&1 | head -20`
- (1x) `echo "=== NODE LS ==="; docker node ls 2>&1 | head -10; echo "=== SERVICE LS ==="; docker service ls 2>&1 | head -40`
- (1x) `echo "=== M1 local ==="; docker ps --format '{{.Names}}\t{{.Status}}' 2>/dev/null | sort; echo; echo "total M1: $(docker ps -q 2>/dev/null | wc -l)"`
- (1x) `echo "=== CONTAINERS M1 ==="; docker ps -a --format "{{.Names}}\t{{.Status}}\t{{.Image}}\t{{.Ports}}" 2>&1 | head -40; echo; echo "=== COMPOSE FILES M1 ==="; docker ps --format '{{.Label "com.docker.compose.project"}}\t{{.Label "com.docker.compose.project.config_files"}}' 2>/dev/null | sort -u | head -20`
- (1x) `echo "== M1 docker =="; docker info --format 'Swarm={{.Swarm.LocalNodeState}} NodeAddr={{.Swarm.NodeAddr}} Containers={{.Containers}} Running={{.ContainersRunning}}' 2>&1|head -2; echo "== running =="; docker ps --format '{{.Names}}\t{{.Image}}\t{{.Status}}' 2>&1 | head -40; echo "== count =="; docker ps -q 2>/dev/null|wc -l; docker ps -aq 2>/dev/null|wc -l`
- (1x) `du -sh /home/turbo/.cache/* 2>/dev/null | sort -rh | head -12; echo "---VARLOG---"; sudo du -sh /var/log 2>/dev/null; echo "---VARLIBDOCKER---"; sudo du -sh /var/lib/docker 2>/dev/null; echo "---TMP---"; du -sh /tmp 2>/dev/null`
- (1x) `docker system prune -a --volumes -f 2>&1 | tail -10 && docker system df 2>/dev/null | head -5`
- (1x) `docker system df 2>/dev/null | head -10`
- (1x) `docker service inspect jarvis_prod_n8n --format '{{json .Endpoint.Ports}}' 2>/dev/null; docker port $(docker ps -q -f name=n8n) 2>/dev/null; ss -tlnp 2>/dev/null | grep -E '5678|n8n'`
- (1x) `docker ps -a --format '{{.Names}}\t{{.Status}}\t{{.RunningFor}}' 2>&1 | wc -l; echo "--- TOUS NOMS ---"; docker ps -a --format '{{.Names}}' 2>&1 | sort; echo "--- CREATED/DEAD/PAUSED ---"; docker ps -a --filter status=created --filter status=dead --filter status=paused --format '{{.Names}}\t{{.Status}}' 2>&1`
- (1x) `docker ps -a --format '{{.Names}}\t{{.Status}}' | grep -i alkym; echo "==="; docker ps --format '{{.Names}}' | grep -iE 'studio|jv-' | head`
- (1x) `docker ps -a --format '{{.Names}}\t{{.State}}\t{{.Status}}\t{{.Image}}' 2>&1 | head -80`
- (1x) `docker ps -a --format '{{.Names}}\t{{.Image}}\t{{.Status}}' | grep -iE 'browser|chrom' | head -3; echo "===CHROME==="; which google-chrome chromium chromium-browser 2>/dev/null | head -2`
- (1x) `docker ps -a --filter status=exited --format '{{.Names}}\t{{.Status}}' 2>&1 | head -60; echo "--- RESTARTING ---"; docker ps -a --filter status=restarting --format '{{.Names}}\t{{.Status}}\t{{.Image}}' 2>&1`
- (1x) `docker ps --format '{{.Names}}|{{.Image}}|{{.Status}}|{{.Ports}}' 2>&1 | head -40; echo "--- PG ---"; docker ps --format '{{.Names}}' | grep -iE 'db|postgres|pg'`
- (1x) `docker ps --format '{{.Names}}\t{{.Status}}\t{{.Image}}' 2>&1 | head -80`
- (1x) `docker ps --format '{{.Names}}' >/dev/null 2>&1 && echo "docker accessible sans sudo" || echo "docker NECESSITE sudo -> le comptage docker sera toujours faux"`
- (1x) `docker node ls 2>&1 | head -20; echo "--- CONTEXTES ---"; docker context ls 2>&1 | head -20; echo "--- SERVICES SWARM ---"; docker service ls 2>&1 | head -30`
- (1x) `docker network ls --format '{{.ID}} {{.Name}} {{.Driver}} {{.Scope}}' | grep -E 'xawufzge|host|jarvis' ; echo "=== resolution reseau antigravity prod ==="; docker network inspect xawufzge5msmjubprj5w7xg4g --format '{{.Name}} {{.Driver}}' 2>&1`
- (1x) `docker network create alkymia-net && docker start jv-studio-s-alkymia-s && sleep 3 && docker ps --format '{{.Names}}\t{{.Status}}' | grep alkym`
- (1x) `docker logs --tail 25 jarvis_prod_cowork-dispatcher.1.wpavsjbf3awvnt4vkdns7h1hv 2>&1 | tail -25`
- (1x) `docker inspect jv-infra-biblio-db --format '{{range .Config.Env}}{{println .}}{{end}}' 2>&1 | grep -i -E 'POSTGRES|DB'`
- (1x) `docker inspect jarvis-redis --format '{{.Name}} | State={{.State.Status}} | Created={{.Created}} | Started={{.State.StartedAt}} | Image={{.Config.Image}}' 2>&1; echo "--- EXIT TIMES ---"; docker ps -a --filter status=exited --format '{{.Names}}' | while read n; do docker inspect "$n" --format '{{.State.FinishedAt}}  {{.State.ExitCode}}  '"$n" 2>/dev/null; done | sort; echo "--- NOW ---"; date -Is`
- (1x) `docker inspect jarvis-redis --format 'Labels={{json .Config.Labels}} ; HostConfig.PortBindings={{json .HostConfig.PortBindings}} ; Cmd={{json .Config.Cmd}} ; Network={{json .HostConfig.NetworkMode}}' 2>&1`
- (1x) `docker exec jv-studio-s-alkymia-s python3 /workspace/scripts/trends_scorer.py fetch --source all --limit 50 2>&1 | tail -8; echo "RC=$?"`
- (1x) `docker exec jv-infra-biblio-db env | grep -E 'POSTGRES_(USER|DB)'; docker exec jarvis_prod_postgres.1.b1kn6i6q1cv86wyi251jlre7e env 2>/dev/null | grep -E 'POSTGRES_(USER|DB)'`
- (1x) `docker compose -f /home/turbo/alkymia/docker-compose.yml ps 2>/dev/null; docker events --since 2m --until 0s --filter type=image 2>/dev/null | head -3; grep -A6 'image\|build' /home/turbo/alkymia/docker-compose.yml | head -15`
- (1x) `df -h / | tail -1; du -xsh /var/log /home/turbo/.cache /tmp /var/lib/docker 2>/dev/null | sort -rh | head -6`
- (1x) `chmod +x ~/jarvis/bin/skillmp.py ; ln -sf ~/jarvis/bin/skillmp.py ~/jarvis/bin/skillmp 2>/dev/null ; echo "=== stats ==="; python3 ~/jarvis/bin/skillmp.py stats ; echo; echo "=== search ==="; python3 ~/jarvis/bin/skillmp.py search docker security -n 6 ; echo; echo "=== installed (avant) ==="; python3 ~/jarvis/bin/skillmp.py installed`
- (1x) `chmod +x ~/jarvis/bin/jarvis-intent.py ; echo "════ CAS 1 : ERP (construire) ════" ; python3 ~/jarvis/bin/jarvis-intent.py "fais-moi un ERP avec React et PostgreSQL" ; echo; echo "════ CAS 2 : incident (réparer) ════" ; python3 ~/jarvis/bin/jarvis-intent.py "le conteneur docker plante et les alertes ne partent plus sur telegram"`
- (1x) `cd ~/jarvis ; echo "=== up d'UN service, sortie brute complète ===" ; timeout 120 docker compose -f docker-compose.jv-entreprise.yml --project-directory ~/jarvis up -d --no-deps jv-infra-prometheus 2>&1 | tr '\r' '\n' | grep -vE "variable is not set|version.*obsolete" | tail -20 ; echo "EXIT=${PIPESTATUS[0]}"`
- (1x) `cd ~/jarvis ; echo "=== bloc networks du compose (nom → subnet) ===" ; awk '/^networks:/{f=1} f' docker-compose.jv-entreprise.yml 2>/dev/null | grep -nE "^  [a-z].*:|subnet:|external:|name:" | head -40`
- (1x) `cd /home/turbo/jarvis/backups/boot-repair-20260806; for p in "timeshift" "home/turbo" "var/lib/docker" "jarvis" "lost+found"; do printf "### %s : " "$p"; grep -a -m 2 -o ".\{0,60\}$p.\{0,40\}" e2fsck-backupos-run2.log 2>/dev/null | head -2 || echo "(absent)"; echo; done`
- (1x) `cd /home/turbo/jarvis-linux && ls | head -30; echo '--- readme ---'; head -40 README.md 2>/dev/null; echo '--- manifests ---'; ls Makefile package.json pyproject.toml docker-compose*.yml 2>/dev/null`
- (1x) `cd /home/turbo/jarvis ; for intent in "cloner un depot git" "construire une image docker" "installer les dependances python"; do ;   echo "── $intent" ;   timeout 90 python3 bin/atomiser-logiques.py reconstruire "$intent" --pas 4 2>&1 | grep -E "^  [0-9]\." | head -4 ; done`
- (1x) `cd /home/turbo/jarvis ; for i in "construire une image docker" "installer les dependances python"; do ;   echo "=== $i ===" ;   timeout 60 python3 bin/atomiser-logiques.py reconstruire "$i" --pas 5 2>&1 | grep -E '^\s+[0-9]\.' | head -5 ; done`
- (1x) `cd /home/turbo/jarvis ; echo "=== reconstruction : 'deployer une app node avec docker' ===" ; timeout 90 python3 bin/atomiser-logiques.py reconstruire "deployer une app node avec docker" --pas 8 2>&1 | head -20`
- (1x) `cd /home/turbo/alkymia && docker compose up -d 2>&1 | tail -5; sleep 3; docker ps --format '{{.Names}}\t{{.Status}}' | grep -i alkym`
- (1x) `cd /home/turbo/Workspaces/jarvis-linux; sed -n '118,150p' infra/docker/nginx.conf; echo "=== resolver ? ==="; grep -n "resolver" infra/docker/nginx.conf`
- (1x) `cd /home/turbo/Workspaces/jarvis-linux; sed -n '1,60p' infra/docker/docker-compose.swarm.yml`
- (1x) `cd /home/turbo/Workspaces/jarvis-linux; echo "=== service loadbalancer ==="; sed -n '/SECTION: NETWORK/,/^  [a-z-]*:$/p' infra/docker/docker-compose.swarm.yml | head -30`
- (1x) `cd /home/turbo/Workspaces/jarvis-linux; echo "=== occurrences \${ ==="; grep -n '\${' infra/docker/docker-compose.swarm.yml; echo; echo "=== styles networks ==="; grep -n -A3 '^    networks:' infra/docker/docker-compose.swarm.yml | head -60; echo; echo "=== chemins absolus /home/turbo ==="; grep -n '/home/turbo' infra/docker/docker-compose.swarm.yml`
- (1x) `cd /home/turbo/Workspaces/jarvis-linux; awk 'NR>=340 && NR<=395' infra/docker/docker-compose.swarm.yml`
- (1x) `cd /home/turbo/Bureau/workflow-gestion/docker && docker compose -f docker-compose.isolated.yml up -d --build 2>&1 | tail -40`
- (1x) `cd /home/turbo/Bureau/workflow-gestion && python3 -m py_compile integrations/telegram/alerter.py docker/../api/server.py core/sentinelle/__main__.py && echo "COMPILE OK" ; echo "=== full alerter.py sanity ===" ; sed -n '1,30p;140,180p' integrations/telegram/alerter.py`
- (1x) `cd /home/turbo/Bureau/workflow-gestion && python3 -m py_compile integrations/telegram/alerter.py && echo "COMPILE OK" ; cd docker && docker compose -f docker-compose.isolated.yml up -d --build 2>&1 | tail -20`
- (1x) `cd /home/turbo && /home/turbo/jarvis/bin/registre-actions.py query "docker conteneur" --couleur destructif -n 3 && /home/turbo/jarvis/bin/registre-actions.py --help 2>&1 | head -12`
- (1x) `bash -n /home/turbo/jarvis/dominos-compiled/dominos/entreprise-stack.sh && echo "✅ syntaxe wrapper OK" ; echo "=== bilan déploiement ===" ; docker ps --format '{{.Names}} | {{.Status}}' 2>/dev/null | grep -E '^jv-' | sort`
- (1x) `LB=69d08844dabc ; echo "=== ce que nginx voit ==="; docker exec $LB sed -n '125,150p' /etc/nginx/nginx.conf ; echo "=== error.log recent ==="; docker exec $LB sh -c 'tail -15 /var/log/nginx/error.log 2>/dev/null' | tail -15`
- (1x) `C=jarvis_prod_openclaw-node.1.rtwpeu9sl9lqtgltfsr432nb0; echo "=== find agents dans conteneur ==="; timeout 8 docker exec $C sh -c 'find / -maxdepth 4 -type d -name "agents" 2>/dev/null | head -10; echo "--openclaw dirs--"; ls -d /root/.openclaw /app /opt/openclaw 2>/dev/null' 2>&1 | head -20; echo "=== process ==="; timeout 5 docker exec $C sh -c 'ps ax 2>/dev/null | head -8' 2>&1 | head -10`
- (1x) `C=$(docker ps --format '{{.Names}}' | grep openclaw-node | head -1); echo "conteneur: $C"; docker exec "$C" node -v; docker exec "$C" sh -c 'cd /app && timeout 20 node openclaw.mjs --version 2>&1 | head -5'; echo "=== gateway port dans conteneur ==="; docker exec "$C" sh -c 'cat /app/start-gateway.sh' 2>&1 | head -20`
